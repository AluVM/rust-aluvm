codecov:
  require_ci_to_pass: no

coverage:
  precision: 1
  round: nearest
  range: "0...95"
  status:
    project:
      default:
        threshold: 1%
        branches:
          - master
    patch:
      default:
        threshold: 1%
        only_pulls: true
